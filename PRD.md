# 小红书评论抽奖工具 - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品名称
**Lottery Box**（小红书评论抽奖工具）

### 1.2 产品定位
一款面向小红书内容创作者的轻量化评论抽奖工具，帮助博主快速、公平地从笔记评论中抽取幸运用户，提升粉丝互动体验。

### 1.3 目标用户
- 小红书博主 / 内容创作者
- 品牌运营人员
- 社群组织者

### 1.4 核心场景
> 博主发布了一条福利笔记（"评论抽3位送样品"），需要在成百上千条评论中公平抽取获奖者。手动复制粘贴到 Excel 再用随机函数，流程繁琐且体验差。

---

## 2. 功能需求

### 2.1 MVP 功能（第一阶段）

| 模块 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| 评论导入 | 文本框批量粘贴评论 | P0 | 用户从小红书复制评论列表，粘贴到文本框 |
| 评论解析 | 智能识别用户名 | P0 | 自动提取评论中的昵称（支持小红书格式） |
| 抽奖配置 | 设置中奖人数 | P0 | 输入 1~N 的中奖名额 |
| 抽奖配置 | 去重设置 | P1 | 同一用户多条评论是否只算一次 |
| 抽奖配置 | 过滤设置 | P2 | 可设置过滤条件（如评论字数、关键词） |
| 抽奖执行 | 随机抽取 | P0 | 公平随机算法，支持动画效果 |
| 结果展示 | 中奖名单 | P0 | 展示中奖用户昵称 |
| 结果展示 | 导出结果 | P1 | 复制中奖名单或导出图片 |

### 2.2 V2 功能（第二阶段）— 基于企业开放平台 API

> 前提：企业已完成小红书开放平台开发者认证，并申请通过「笔记评论 API」权限

| 模块 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| 授权登录 | 小红书 OAuth 授权 | P0 | 用户扫码授权，获取 access_token |
| 笔记选择 | 拉取笔记列表 | P0 | 自动拉取授权账号下的笔记列表 |
| 评论获取 | API 拉取评论 | P0 | 输入笔记 ID，自动分页拉取全部评论 |
| 评论获取 | 增量更新 | P1 | 支持只拉取新增评论，避免重复抽奖 |
| 高级过滤 | 关注检测 | P1 | 检测评论者是否关注了博主（需额外权限） |
| 高级过滤 | 互动质量评分 | P2 | 基于评论内容质量做加权或过滤 |
| 结果存证 | 抽奖记录上链/存证 | P2 | 记录抽奖时间、参与人数、随机种子，保证可追溯 |

### 2.3 扩展功能（后续迭代）
- **多奖品设置**：一等奖、二等奖、三等奖分别设置名额
- **定时抽奖**：预设开奖时间，自动执行
- **历史记录**：云端保存抽奖记录，支持再次验证
- **分享卡片**：生成小红书风格的分享图片

---

## 3. 用户流程

### 3.1 MVP 流程（手动粘贴模式）

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  打开工具    │────▶│  粘贴评论    │────▶│  配置规则    │
└─────────────┘     └─────────────┘     └─────────────┘
                                               │
                                               ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  导出/分享   │◀────│  查看结果    │◀────│  开始抽奖    │
└─────────────┘     └─────────────┘     └─────────────┘
```

**Step 1: 粘贴评论**
- 用户从小红书 APP 复制评论内容
- 粘贴到大文本框中
- 系统实时预览解析结果（显示识别到的用户数）

**Step 2: 配置规则**
- 设置中奖人数（默认 1，范围 1-100）
- 去重开关（默认开启：同一昵称多条评论只保留一条）
- （可选）设置过滤关键词

**Step 3: 执行抽奖**
- 点击"开始抽奖"按钮
- 播放滚动动画
- 动画停止，展示中奖用户

**Step 4: 结果处理**
- 展示中奖名单（带序号）
- 一键复制名单
- 生成分享图片

### 3.2 V2 流程（API 自动模式）

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  打开工具    │────▶│  扫码授权    │────▶│  选择笔记    │
└─────────────┘     └─────────────┘     └─────────────┘
                                               │
                                               ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  导出/分享   │◀────│  查看结果    │◀────│  配置+抽奖   │
└─────────────┘     └─────────────┘     └─────────────┘
```

**Step 1: 授权登录**
- 用户点击"连接小红书"
- 扫码完成 OAuth 授权
- 获取 access_token（有效期 2 小时，需刷新）

**Step 2: 选择笔记**
- 自动拉取授权账号下的笔记列表
- 用户选择要抽奖的笔记
- 系统自动拉取该笔记的全部评论（分页处理）

**Step 3: 配置与抽奖**
- 同 MVP 的配置规则
- 基于 API 获取的真实用户 ID 进行去重（更精准）

**Step 4: 结果处理**
- 展示中奖名单（带用户头像、昵称）
- 支持导出详细结果（含用户 ID，方便核对）
- 保存抽奖记录到本地/云端

---

## 4. 界面设计

### 4.1 页面结构

```
┌─────────────────────────────────────┐
│           顶部 Header                │
│    小红书评论抽奖工具  🎲            │
├─────────────────────────────────────┤
│                                     │
│    [ 手动粘贴 ] [ API自动 ]          │  ← 模式切换 Tab
│                                     │
│    ┌───────────────────────────┐    │
│    │                           │    │
│    │     评论粘贴区域           │    │
│    │   （占位提示 + 粘贴引导）   │    │
│    │                           │    │
│    └───────────────────────────┘    │
│                                     │
│    已识别 156 位用户                │
│                                     │
│    ┌───────────────────────────┐    │
│    │  中奖人数: [ 3 ] 人        │    │
│    │  [✓] 同一用户去重          │    │
│    │  [ ] 过滤关键词...         │    │
│    └───────────────────────────┘    │
│                                     │
│    ┌───────────────────────────┐    │
│    │       🎰 开始抽奖          │    │
│    └───────────────────────────┘    │
│                                     │
│    ┌───────────────────────────┐    │
│    │      抽奖结果区域          │    │
│    │   🎉 恭喜以下用户中奖！    │    │
│    │   1. @用户名A              │    │
│    │   2. @用户名B              │    │
│    │   3. @用户名C              │    │
│    │   [复制名单] [保存图片]    │    │
│    └───────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│           Footer 说明               │
└─────────────────────────────────────┘
```

### 4.2 视觉风格
- **整体风格**：小红书品牌色（#FF2442 为主色调）
- **圆角卡片**：16px 圆角，柔和阴影
- **字体**：系统默认无衬线字体
- **动效**：抽奖时的滚动动画、中奖时的庆祝动效（彩带/闪烁）

---

## 5. 技术方案

### 5.1 技术选型

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vanilla JS（MVP）/ React（V2） | MVP 轻量为主，V2 引入 React 管理状态 |
| 样式 | Tailwind CSS | 快速实现响应式布局 |
| 构建工具 | Vite | 快速开发和构建 |
| 后端（V2） | Node.js + Express / Cloudflare Workers | 处理 OAuth、API 代理、刷新 token |
| 部署 | 国内服务器 / 国内 CDN（前端静态托管 + Node.js 后端服务） | Nginx 托管静态资源，V2 后端部署为国内服务器 Node.js 服务 |

### 5.2 核心算法

**评论解析逻辑**
```javascript
// 小红书评论常见格式：
// "用户名\n评论内容\n点赞数\n回复"
// 需要支持多种粘贴格式，灵活解析用户名

function parseComments(text) {
  // 按行分割，提取看起来像用户名的部分
  // 支持格式：
  // - 纯用户名列表（一行一个）
  // - 小红书完整复制格式（用户名+内容+点赞）
  // - @用户名 格式
}
```

**随机抽取算法**
```javascript
function drawWinners(users, count, deduplicate = true) {
  // 1. 去重（如开启）
  // 2. Fisher-Yates 洗牌算法
  // 3. 取前 N 个
}
```

### 5.3 V2 API 对接方案

**OAuth 流程**
```
用户点击授权 → 跳转小红书授权页 → 用户扫码确认 
→ 回调携带 auth_code → 后端换取 access_token + refresh_token
→ 前端存储 token → 调用笔记/评论 API
```

**API 调用**
```javascript
// 获取笔记评论
GET https://api.xiaohongshu.com/v2/notes/{note_id}/comments
Headers: Authorization: Bearer {access_token}
Query: page, page_size

// 分页处理：cursor 翻页，直到 has_more === false
```

### 5.4 数据安全
- **MVP**：纯前端实现，评论数据不上传服务器
- **V2**：
  - access_token 存储在服务端 session，不暴露给前端
  - API 请求通过服务端代理，避免前端暴露 App Secret
  - 用户授权可随时撤销

---

## 6. 开放平台准入准备（V2）

### 6.1 所需资质
- 企业营业执照
- 企业法人身份证
- 应用场景说明文档（用于 API 权限申请）

### 6.2 申请流程
1. 访问 https://open.xiaohongshu.com/ 注册企业开发者账号
2. 完成企业资质认证（免费）
3. 创建应用，填写应用名称、类型（选择"数据分析工具"或"内容管理工具"）
4. 申请「笔记详情 API」和「评论 API」权限
5. 提交场景说明："用于帮助小红书博主从笔记评论中公平抽取互动用户，提升粉丝运营效率"
6. 等待审核（通常 1-5 个工作日）
7. 审核通过后获取 App Key、App Secret

### 6.3 费用
- **开发者认证**：免费
- **API 调用**：暂时免费（有频率限制，每分钟 ≤100 次）
- **注意**：平台保留未来收费的权利

---

## 7. 项目结构

```
lottery-box/
├── index.html              # 主页面
├── css/
│   └── style.css           # 样式文件
├── js/
│   ├── parser.js           # 评论解析器
│   ├── lottery.js          # 抽奖逻辑
│   ├── ui.js               # 界面交互
│   └── api.js              # V2: API 对接
├── assets/
│   └── confetti.js         # 庆祝动效
├── api/                    # V2: 后端服务
│   ├── auth.js             # OAuth 处理
│   ├── proxy.js            # API 代理
│   └── token.js            # Token 管理
└── README.md               # 使用说明
```

---

## 8. 非功能需求

### 8.1 性能
- 解析 1000 条评论 < 1 秒
- 抽奖动画流畅，60fps
- API 分页拉取评论时显示进度条

### 8.2 兼容性
- 支持主流浏览器（Chrome、Safari、Edge、微信内置浏览器）
- 响应式布局，支持手机和 PC 端

### 8.3 可访问性
- 按钮和输入框有清晰的焦点状态
- 颜色对比度符合 WCAG 标准

---

## 9. 里程碑

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| **MVP** | 1-2 天 | 可运行的静态页面，支持粘贴-配置-抽奖-复制全流程 |
| **优化** | 3-5 天 | 动画优化、分享图片生成、UI 打磨 |
| **V2 API** | 2-3 周 | 完成开放平台企业认证、API 对接、自动拉取评论功能 |
| **迭代** | 后续 | 多奖品、定时抽奖、历史记录云端化 |

---

## 10. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 小红书开放平台 API 权限审核不通过 | 高 | MVP 手动粘贴方案保底，审核被拒不影响核心功能 |
| 开放平台未来开始收费 | 中 | 保留手动粘贴模式作为 fallback |
| API 频率限制导致大量评论拉取慢 | 中 | 分页拉取 + 进度条展示，超限后提示用户稍等 |
| 评论格式多样解析不准 | 中 | 支持多种格式，提供手动调整入口 |
| 用户授权后 token 过期 | 低 | 自动刷新 token，过期后引导重新授权 |

---

## 11. 附录

### 11.1 小红书评论常见格式示例

```
用户昵称1
这条评论内容很不错
12
用户昵称2
我也想要！
5
用户昵称3
试一试
1
```

### 11.2 开放平台 API 参考

| 接口 | 地址 | 用途 |
|------|------|------|
| 授权 | `https://open.xiaohongshu.com/oauth/authorize` | 获取 auth_code |
| 换取 Token | `https://open.xiaohongshu.com/oauth/token` | 换取 access_token |
| 笔记评论 | `https://api.xiaohongshu.com/v2/notes/{note_id}/comments` | 拉取评论列表 |

### 11.3 参考工具
- 微博抽奖平台
- 微信抽奖小程序
- Comment Picker 等海外工具
